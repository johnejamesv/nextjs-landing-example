/**
 * Design Tokens - Central Exports
 *
 * All design system tokens exported from a single module.
 * Import as: `import { theme, colors, gradients } from '@/lib/design-tokens'`
 */

export { theme, colors, gradients, shadows, blur, radius, animation } from './theme';
export type { Theme } from './theme';
